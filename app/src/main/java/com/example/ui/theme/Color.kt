package com.example.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * Purple Launcher semantic accent tokens.
 *
 * These are identity/profile accents only.
 * They are NOT global launcher surfaces.
 *
 * Structural colors belong to LauncherAppearance so the launcher
 * can support neutral, dark, light, dynamic, and wallpaper-derived
 * compositions without forcing purple everywhere.
 */

// Brand identity
val PurpleAccent = Color(0xFFA855F7)
val PurpleAccentStrong = Color(0xFF7C3AED)
val PurpleAccentSoft = Color(0xFFD8B4FE)

// Supporting semantic accents
val CyanAccent = Color(0xFF38BDF8)
val EmeraldAccent = Color(0xFF34D399)
val AmberAccent = Color(0xFFFBBF24)

// Neutral utility colors.
// Prefer LauncherAppearance for structural UI whenever possible.
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
